# Aktifkan Rewrite Engine
RewriteEngine On

# Jangan terapkan rewrite jika file atau direktori sudah ada
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite rule: Tambahkan .php secara internal
RewriteRule ^([^\.]+)$ $1.php [L]
